import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'you-got-this',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
